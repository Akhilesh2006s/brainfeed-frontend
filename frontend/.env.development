# Used when you run `npm run dev`. Must restart Vite after changing.
# Deployed API (default — matches frontend/.env.production):
VITE_API_URL=https://brainfeed-backend-production-ab6d.up.railway.app

# Local backend only — uncomment this and comment the line above:
# VITE_API_URL=http://localhost:3001
